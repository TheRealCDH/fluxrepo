apiVersion: source.toolkit.fluxcd.io/v1
kind: HelmRepository
metadata:
  name: headlamp
  namespace: flux-system
spec:
  interval: 1h
  url: https://kubernetes-sigs.github.io/headlamp/
---
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: headlamp
  namespace: flux-system
spec:
  interval: 1h
  chart:
    spec:
      chart: headlamp
      version: ">=0.21.0"
      sourceRef:
        kind: HelmRepository
        name: headlamp
        namespace: flux-system
  install:
    createNamespace: true
  values:
    service:
      type: NodePort
      port: 80
      nodePort: 30015
    config:
      watchPlugins: true
    # Correct fields for volumes and volumeMounts
    volumes:
      - name: plugins-dir
        emptyDir: {}
    volumeMounts:
      - name: plugins-dir
        mountPath: /headlamp/plugins
    initContainers:
      - name: install-kubevirt
        image: alpine:latest
        command: ["/bin/sh", "-c"]
        args:
          - |
            mkdir -p /headlamp/plugins/kubevirt
            wget -qO- https://github.com/headlamp-k8s/plugins/releases/download/kubevirt-0.2.0/kubevirt-0.2.0.tar.gz | tar -xz -C /headlamp/plugins/kubevirt --strip-components=1
        volumeMounts:
          - name: plugins-dir
            mountPath: /headlamp/plugins
    pluginsManager:
      enabled: false
