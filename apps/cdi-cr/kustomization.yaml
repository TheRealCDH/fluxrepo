apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - cdi-cr.yaml
patches:
  - target:
      kind: CDI
      name: cdi
    patch: |-
      - op: add
        path: /spec/config/featureGates/-
        value: "DisableVolumePopulator"
