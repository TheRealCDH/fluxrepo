apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # 1. Sync Logic (Folder pointers)
  - sync-manifests.yaml

  # 2. Workloads & Dashboards
  - example-app.yaml
  - headlamp.yaml
  - headlamp-auth.yaml
