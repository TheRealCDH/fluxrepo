apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: custom-types
  namespace: flux-system
spec:
  dependsOn:
    - name: kubevirt-operator
  interval: 2h
  prune: true
  sourceRef:
    kind: GitRepository
    name: flux-system
  path: ./apps/custom-types
