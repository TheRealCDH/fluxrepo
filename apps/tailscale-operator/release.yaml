apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: tailscale-operator
  namespace: flux-system
spec:
  interval: 1h
  chart:
    spec:
      chart: tailscale-operator
      version: ">=1.74.0"
      sourceRef:
        kind: HelmRepository
        name: tailscale
        namespace: flux-system
  install:
    createNamespace: true
  values:
    # Placeholder values. OAuth client ID/Secret would go here or in a Secret.
    # operatorConfig:
    #   hostname: "flux-cluster"
